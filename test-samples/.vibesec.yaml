# .vibesec.yaml — example policy for the test-samples directory
# Copy this to your workspace root and rename it .vibesec.yaml

presets:
  - vibesec:default

severity:
  minSeverity: warning
  # overrides:
  #   vibesec.hardcoded-secret: error

# files:
#   exclude:
#     - "**/*.test.py"
#     - "**/node_modules/**"
